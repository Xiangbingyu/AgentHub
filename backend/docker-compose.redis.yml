services:
  redis:
    image: redis:7-alpine
    container_name: agenthub-backend-redis
    ports:
      - "6382:6379"
    volumes:
      - redis-data:/data
    command: ["redis-server", "--appendonly", "yes"]

volumes:
  redis-data:
